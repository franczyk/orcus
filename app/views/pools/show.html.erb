<p>
  <b>Name:</b>
  <%=h @pool.name %>
  <br>
  <br>
  Host List:<br/>
  <ul>
  <% @pool.hosts.each do |host| %>
    <li><%=h link_to(host.name , :controller => "hosts", :action => "show", :id => host.id) %></li>
  <% end %>
</ul>
Count: <%=h @pool.hosts.length.to_s %>
</p>


<%= link_to 'Edit', edit_pool_path(@pool) %> |
<%= link_to 'Back', pools_path %>
