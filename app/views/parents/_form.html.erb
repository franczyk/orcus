<%= form_for(@parent) do |f| %>
  <% if @parent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parent.errors.count, "error") %> prohibited this parent from being saved:</h2>

      <ul>
      <% @parent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :parent_chain_id %><br />
    <%= f.text_field :parent_chain_id %>
  </div>
  <div class="field">
    <%= f.label :child_chain_id %><br />
    <%= f.text_field :child_chain_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
